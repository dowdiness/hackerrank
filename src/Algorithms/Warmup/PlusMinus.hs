module Algorithms.Warmup.PlusMinus where

-- Plus Minus
-- https://www.hackerrank.com/challenges/plus-minus

-- solve :: [Int] -> [Fractional]
-- solve (x:xs) = [pos/x, neg/x, zero/x]
--   where
--     pos  = length . filter (> 0) xs
--     neg  = length . filter (< 0) xs
--     zero = length . filter (== 0) xs

-- main :: IO ()
-- main = interact $ unlines . map show . solve . map read . words
